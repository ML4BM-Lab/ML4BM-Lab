# Partials (reference copies)

The live header and footer are defined in **`js/layout.js`**. Edit that file to change the navigation or footer site-wide.

The `header.html` and `footer.html` files here are reference copies — if you edit them, copy the same changes into `js/layout.js` so they appear on the site.
